# pnpm 10 は .npmrc から認証とレジストリ以外の設定を読まないため、
# pnpm の設定はこのファイルに書く。
# packages を省略するとルートパッケージのみが対象になるため、
# モノレポでなくても設定ファイルとして置ける

# engines と違う Node では install を止める。
# 環境差に気づくのが CI まで遅れるのを防ぐ
engineStrict: true
