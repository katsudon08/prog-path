/**
 * 「未分類」フォルダの予約 ID。常に 1 つ存在し削除・リネーム不可、
 * 起動時に存在保証される（→ docs/db-design.md 3.1, 7 / #179）。
 * 通常フォルダの ID は UUID(v4) で採番するため、v4 では生成され得ない
 * nil UUID を予約センチネルとして用いる。
 */
export const UNCATEGORIZED_FOLDER_ID = "00000000-0000-0000-0000-000000000000";

/**
 * 「チュートリアル」フォルダの予約 ID。授業導入用の教材迷路をまとめる専用フォルダで、
 * 未分類フォルダと同格に起動時へ存在保証される。ただし可否は非対称で、
 * **リネーム不可・迷路の出入り不可だが、フォルダ自体の削除は可能**（消しても次の存在保証で戻る）。
 * 判定は entities/folder が本 ID との一致で行う（→ docs/db-design.md 7 / #192）。
 * 通常採番と区別する固定 v4 UUID を予約センチネルとして用いる。
 */
export const TUTORIAL_FOLDER_ID = "10000000-0000-4000-8000-000000000000";
