/**
 * Public API — `widgets/ar-stage` スライス
 *
 * AR 描画＋実行ブロック。カメラ背景＋3D 重畳＋オーバーレイ群を束ねる
 * （maze-simulation / entities(maze, robot) / shared(camera)）。
 *
 * 他スライスからの import はこの index.ts 経由のみ。`export * from` は使わず
 * 公開対象を個別に明示し、公開シンボルは実装着手時に追加する
 * （→ docs/directory-structure.md 2.2）。
 */
export {};
