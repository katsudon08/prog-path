# 移行で破棄予定の現行実装（.katsudon08/今後の方針.md 参照）。移行完了時にこの4行を消す
/src/
/app/
/pages/
/main.js

# アセット（robot.gltf は 42MB、typeface.json は three.js のフォントデータ。整形の意味がない）
/public/

# Markdown は当面対象外（.katsudon08/ と docs/ の既存メモが表の整列で大量に差分化するため）
*.md

# 生成物・ロックファイル
/.next/
/out/
/dist/
/build/
/package-lock.json
tsconfig.tsbuildinfo
next-env.d.ts
