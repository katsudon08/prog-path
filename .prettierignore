# 検査対象の範囲は docs/ci.md を参照。移行完了時にこの4行を消す
# src/ を丸ごと除外すると globals.css も外れてしまうため拡張子で切っている
# (gitignore 仕様により、除外したディレクトリ配下を ! で戻すことはできない)
# src/ 配下は .ts / .tsx / .css の3種類しかないので、これで CSS だけが対象に残る
/src/**/*.ts
/src/**/*.tsx
/main.js

# アセットなので整形の対象外にする
/public/

# md の書き方は指定しない (個人の書き方に委ねる)
*.md

# 生成物・ロックファイル
/out/
/dist/
/build/
/pnpm-lock.yaml
tsconfig.tsbuildinfo
