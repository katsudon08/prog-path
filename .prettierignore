# 検査対象の範囲は docs/ci.md を参照。
# 旧コードは書き換えない前提なので整形もしない。
# 移行が終わって legacy が消えたら /src/legacy/ の行を消す
/src/legacy/
# Electron のエントリ。移行とは無関係にここへ残る
/main.js

# アセットなので整形の対象外にする
/public/

# md の書き方は指定しない (個人の書き方に委ねる)
*.md

# 生成物・ロックファイル
/out/
/dist/
/build/
/pnpm-lock.yaml
tsconfig.tsbuildinfo
